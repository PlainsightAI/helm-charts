apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "object-filter.fullname" . }}-test-connection"
  labels:
    {{- include "object-filter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "object-filter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
