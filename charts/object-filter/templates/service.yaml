apiVersion: v1
kind: Service
metadata:
  name: {{ include "object-filter.fullname" . }}
  labels:
    {{- include "object-filter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "object-filter.selectorLabels" . | nindent 4 }}
