apiVersion: v1
kind: Service
metadata:
  name: {{ include "filter-core.fullname" . }}
  labels:
    {{- include "filter-core.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "filter-core.selectorLabels" . | nindent 4 }}
