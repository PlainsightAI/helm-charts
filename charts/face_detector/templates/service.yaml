apiVersion: v1
kind: Service
metadata:
  name: {{ include "face_detector.fullname" . }}
  labels:
    {{- include "face_detector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "face_detector.selectorLabels" . | nindent 4 }}
