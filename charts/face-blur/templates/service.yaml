apiVersion: v1
kind: Service
metadata:
  name: {{ include "face-blur.fullname" . }}
  labels:
    {{- include "face-blur.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "face-blur.selectorLabels" . | nindent 4 }}
