apiVersion: v1
kind: Service
metadata:
  name: {{ include "face-detector.fullname" . }}
  labels:
    {{- include "face-detector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "face-detector.selectorLabels" . | nindent 4 }}
