apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "face-detector.fullname" . }}-test-connection"
  labels:
    {{- include "face-detector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "face-detector.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
